{
  "name": "custom-mcp-server",
  "version": "1.0.0",
  "description": "Sequential Thinking MCP Server with Supergateway integration",
  "main": "src/server.py",
  "scripts": {
    "start": "npx -y supergateway --port 8001 --stdio \"npx @modelcontextprotocol/server-sequential-thinking\"",
    "dev": "nodemon --exec npm start"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^0.2.0",
    "@modelcontextprotocol/server-sequential-thinking": "^0.2.0",
    "supergateway": "latest"
  },
  "devDependencies": {
    "nodemon": "^3.0.0"
  }
}
